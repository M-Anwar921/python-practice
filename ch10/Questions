Chapter 9 — File I/O
not used in LeetCode — but useful for projects
Q1
Write a program that creates a file called "notes.txt" and writes 5 lines of text to it.

easy
Q2
Read a text file and count the total number of words in it.

Hint: read(), then split() on whitespace
easy
Q3
Append 3 new lines to an existing file without overwriting its content.

Hint: open with mode "a" to append
easy
Q4
Read a file and print only lines that contain the word "Python" (case-insensitive).

medium
Q5
Write a program that reads a file, counts frequency of each word, and writes the results to a new file.

medium