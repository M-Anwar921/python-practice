Chapter 8 — Functions & recursion
LeetCode wraps every solution in a function
Q1
Write a function that takes a list and returns the sum, average, min, and max as a tuple.

stats([4, 7, 2, 9, 1])
# (23, 4.6, 1, 9)
easy
Q2
Write a function to calculate factorial of n using recursion.

factorial(5) → 120
factorial(0) → 1
Hint: base case is n == 0 or n == 1
easy
Q3
Write a function that checks if a number is prime. Then use it to filter prime numbers from a list.

nums = [2, 3, 4, 5, 6, 7, 8, 9, 10]
# primes: [2, 3, 5, 7]
medium
Q4
Write a recursive function to compute the nth Fibonacci number.

fib(6) → 8
# sequence: 0,1,1,2,3,5,8...
Hint: fib(n) = fib(n-1) + fib(n-2)
medium
Q5
Write a function that takes *args and returns the product of all numbers passed to it.

multiply(2, 3, 4) → 24
multiply(5, 5)    → 25
easy
Q6
Write a function two_sum(nums, target) that returns indices of two numbers that add to target — exactly like LeetCode.

two_sum([2, 7, 11, 15], 9) → [0, 1]
LeetCode #1 - Two Sum
hard
Q7
Write a recursive function to reverse a string.

reverse("hello") → "olleh"
Hint: return reverse(s[1:]) + s[0]
medium
Q8
Write a function that takes a list and a target, and uses binary search to return the index. Return -1 if not found.

binary_search([1,3,5,7,9], 7) → 3
binary_search([1,3,5,7,9], 4) → -1
medium